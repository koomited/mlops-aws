apiVersion: 1

# list of datasources to insert/update
# available in the database
datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: db.:5432
    database: pg_database
    user: postgres
    secureJsonData:
      password: password
    jsonData:
      sslmode: disable
